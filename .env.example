# --- DATABASE CONFIGURATION ---
# The username for the database
DB_USER=retain_user

# The password for the database (Change this!)
DB_PASSWORD=change_me_please

# The name of the database to create
DB_NAME=retain_db

# --- APPLICATION CONFIGURATION ---
# Port the backend runs on internally
PORT=8080

# URL where the frontend is accessible (Used for CORS)
# For local dev/docker: http://localhost:3000
# For production: https://your-domain.com
FRONTEND_URL=http://localhost:3000

# URL where the backend is accessible from the browser
# For local dev/docker: http://localhost:8080
# For production: https://api.your-domain.com
VITE_API_BASE_URL=http://localhost:8080

# --- SECRETS (If your app uses Auth) ---
# You likely need a secret for JWTs or Sessions. 
# Uncomment and add this if your backend code uses it.
# JWT_SECRET=random_string_here